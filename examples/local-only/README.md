# Local-only TekMemo example

This example writes a temporary `.tekmemo/` workspace using `tekmemo` and `@tekmemo/fs`.

Run it from the repository root after installing dependencies and building packages:

```bash
pnpm build
pnpm --filter @tekmemo/example-local-only start
```
